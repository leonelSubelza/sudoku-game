export enum DIFFICULTS {
  EASY = 38,
  NORMAL = 36,
  HARD = 30,
}

export enum CellStatus {
  NORMAL = "NORMAL",
  SELECTED = "SELECTED",
  EQUAL = "EQUAL",
  SHADING = "SHADING",
  ERROR = "ERROR",
}

export enum CellValueStatus {
  DEFAULT = "DEFAULT",
  CORRECT = "CORRECT",
  INCORRECT = "INCORRECT",
}

export enum GameStatus {
  PLAYING = "PLAYING",
  RESET = "RESET",
  NEW_GAME = "NEW_GAME",
  FINISHED = "FINISHED",
  PAUSED = "PAUSED",
}